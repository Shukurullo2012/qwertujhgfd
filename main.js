let name = prompt("Введите ваше имя:");
let letter = prompt("Введите букву для поиска:");

if (includes(letter)) {
    alert(`${letter}есть в имени` );
} else {
    alert(`${letter}есть в имени` );
}